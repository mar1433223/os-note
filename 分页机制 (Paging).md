## 第18章：分页

---

### 为什么需要分页

分段的段大小不固定，分配释放多次后产生外部碎片，没有连续空间装新段。分页把内存切成**固定大小**的块，从根本上消除外部碎片。

---

### 两个核心概念

- **页（page）**：虚拟地址空间切成固定大小的块（一般4KB）
- **页帧（frame）**：物理内存也切成同样大小的块

虚拟页通过**页表**映射到物理页帧。

---

### 地址转换

虚拟地址分两部分：

```
虚拟地址 = VPN（虚拟页号）+ 页内偏移
```

转换过程：

```
VPN → 查页表 → PFN（物理页帧号）
物理地址 = PFN + 页内偏移（偏移量不变）
```

---

### 执行一条指令的完整流程

```
1. PC里的虚拟地址 → 查页表 → 取出指令
2. 指令涉及内存访问 → 再查页表 → 取出数据
```

每条涉及内存的指令**最少访问两次内存**，性能有损耗 → 引出TLB。

---

### 和分段对比


---

### 缺陷

- 每次访存要多查一次页表，**速度慢** → TLB解决（第19章）
- 页表本身占用大量内存 → 多级页表解决（第20章）
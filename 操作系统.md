# 🧭 操作系统复习大纲 (OSTEP)

> [!INFO] 核心视角 操作系统是一个并发的、面向硬件的 **状态机**。 
> 本笔记基于《操作系统导论》(OSTEP) + 南京大学蒋炎岩课程构建，分为三大核心支柱。

---

## 📌 学习进度追踪

| 模块        | 状态     | 备注           |
| --------- | ------ | ------------ |
| 虚拟化 - CPU | 🟡 进行中 | 第14章学完，12章没看 |
| 虚拟化 - 内存  | ⬜ 未开始  |              |
| 并发        | 🟢 有基础 | Java 多线程已学   |
| 持久化       | ⬜ 未开始  |              |
| Lab 实验    | 🟡 进行中 |              |

---

## 🏗️ 1. 虚拟化 (Virtualization)

> [!NOTE] 核心问题 操作系统如何让每个程序"以为"自己独占 CPU 和内存？

### 🧠 CPU 虚拟化（时间切片）

- [[机制与策略]]：底层的上下文切换（机制）与高层的调度算法（策略）。
- [[进程 (Process)]]：运行中程序的抽象；状态机视角下的 PCB 与状态转换。
- [[系统调用与陷阱 (Trap)]]：用户态 → 内核态的受控转移，Limited Direct Execution。
- [[上下文切换 (Context Switch)]]：寄存器现场的保存与恢复。
- [[CPU 调度算法]]：FIFO、SJF、STCF、RR、MLFQ — 各算法的权衡与适用场景。
-  [[比例份额调度]]：彩票调度与步长调度 — 按比例分配CPU时间而非优化周转/响应时间。

### 💾 内存虚拟化（空间隔离）

- [[地址空间 (Address Space)]]：每个进程眼中的连续内存；栈、堆、代码段布局。
- [[地址转换机制]]：Base & Bounds → 分段 → 分页的演化逻辑。
- [[分页机制 (Paging)]]：页表结构、虚拟地址拆分、解决外部碎片。
- [[多级页表]]：为什么单级页表太大？x86-64 四级页表简介。
- [[TLB (翻译后备缓冲器)]]：硬件级缓存加速地址转换；TLB Miss 处理流程。
- [[内存分配原理]]：`malloc` 背后的空闲链表、伙伴系统；`sbrk` 系统调用。

---

## ⚡ 2. 并发 (Concurrency)

> [!WARNING] 核心问题 多个执行流共享状态时，如何保证程序行为的确定性？

### 🧵 基础概念

- [[线程 (Thread)]]：共享地址空间的执行流；与进程的区别。
- [[临界区与竞态条件]]：并发 Bug 的根源；可见性 vs 原子性问题。
- [[原子指令 (Test-and-Set / CAS)]]：硬件提供的最小原子操作，是所有锁的基础。

### 🛠️ 同步原语（Java / C 对照）

- [[锁 (Locks)]]：自旋锁 → 互斥量；硬件原语如何支撑软件锁。
- [[条件变量 (Condition Variables)]]：`wait()` / `notify()` 的 OS 底层语义。
- [[信号量 (Semaphores)]]：PV 操作；生产者-消费者、读者-写者经典问题。
- [[死锁 (Deadlock)]]：四个必要条件；预防、避免、检测与恢复策略。

### 🔗 Java ↔ OS 概念映射

|OS 原语|Java 实现|备注|
|---|---|---|
|mutex|`synchronized` / `ReentrantLock`|可重入锁|
|condition variable|`Object.wait()` / `Condition`|需在锁内调用|
|semaphore|`java.util.concurrent.Semaphore`|可设许可数|
|thread|`Thread` / `ExecutorService`|JVM 线程 = OS 线程（大多数 JVM）|
|CAS|`AtomicInteger` 等|底层 `LOCK CMPXCHG`|

---

## 💽 3. 持久化 (Persistence)

> [!FAQ] 核心问题 掉电之后，数据如何安全、完整地留在硬盘上？

### 💿 硬件与 I/O

- [[I/O 设备交互]]：轮询 vs 中断 vs DMA；设备驱动的抽象层。
- [[磁盘与 SSD 原理]]：磁盘寻道/旋转延迟；SSD 的 FTL 与写放大；物理特性决定调度算法。
- [[磁盘调度算法]]：SSTF、SCAN、C-SCAN。

### 📁 文件系统

- [[文件与目录的 API]]：`open`、`read`、`write`、`fsync`、`lseek`；文件描述符本质。
- [[文件系统实现 (VSFS)]]：inode、数据块、位图；`ls` 背后发生了什么。
- [[FFS 与局部性优化]]：为什么 VSFS 慢？块组 + 局部性策略。
- [[崩溃恢复与日志 (Journaling)]]：write ordering、write-ahead log；`fsck` vs journal。

---

## 💻 4. 实验与实战 (Lab)

> [!TIP] 动手写代码才是真理。光看书永远到不了第二层。

- [[Linux 移动硬盘环境配置]]：Arch Linux on SSD；挂载、fstab、环境变量踩坑记录。
- [[JYY Lab 记录]]：NJU OS L1 / L2 实验的思路、踩坑与心得。
- [[Java 并发模拟代码]]：用 Java 手写自旋锁、有界队列、状态机，验证 OS 并发概念。
- [[系统调用实验]]：用 `strace` / `ltrace` 观察真实程序的系统调用序列。

---

## 📚 参考资源

| 资源                                                              | 用途       |
| --------------------------------------------------------------- | -------- |
| [OSTEP 在线版](https://pages.cs.wisc.edu/~remzi/OSTEP/)            | 主教材      |
| [蒋炎岩 NJU OS 2022](https://www.bilibili.com/video/BV1Cm4y1d7Ur/) | 主视频课     |
| `man` 手册页                                                       | 系统调用速查   |
| [OSDev Wiki](https://wiki.osdev.org/)                           | 底层实现细节参考 |